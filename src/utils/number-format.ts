export const decimetresToMeters = (decimeters?: number) =>
  ((decimeters || 0) * 0.1).toFixed(2);

export const hectogramsToKilograms = (hectograms?: number) =>
  ((hectograms || 0) * 0.1).toFixed(2);
